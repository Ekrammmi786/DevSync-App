# DevSync-App

Real-time collaboration and meet platform for developers and students.

## Setup

### Backend
```bash
cd backend
cp .env.example .env
# fill in your secrets in .env
npm install
npm run dev
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```

**Note:** Never commit `.env` or `node_modules` — they are gitignored.
